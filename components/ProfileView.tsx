import React from 'react';

const ProfileView: React.FC<{setActiveTab: any}> = () => {
  // This component's content has been merged into DashboardView.
  // It is kept temporarily to avoid breaking imports but should be removed in a future refactor.
  return null;
};

export default ProfileView;